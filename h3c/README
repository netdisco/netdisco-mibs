no date reference in these files:
hh3c-common-system.mib
hh3c-oid.mib
hh3c-product-id.mib
hh3c-splat-smonext.mib

have manually added "LAST-UPDATED" entries so our update scripts don't
error out on these. you'll need to add these entries manually to these
files with the correct timestamp before running the update scripts.

the can be identified with the following string:
--   comment below added by netdisco-devs to process update for netdisco-mibs
--   LAST-UPDATED "201609020000Z" Sep 02, 2016

be sure to add those entries before running the prepmibs stage.
